# CDC PLACES API Token (optional but recommended)
# Get token from: https://data.cdc.gov/ → Sign in → Profile → Developer Settings
# Without token: Rate limited to 1,000 requests/hour
# With token: Rate limited to 10,000 requests/hour
CDC_APP_TOKEN=

# Data Warehouse
DUCKDB_PATH=data/warehouse.duckdb

# Data Extraction Configuration
TARGET_STATES=  # e.g., WI,MN,IL,IA,MI or leave blank for all states
TARGET_YEAR=2023  # Latest available year

# Logging
LOG_LEVEL=INFO

# dbt Configuration
DBT_PROJECT_DIR=dbt_project
DBT_PROFILES_DIR=~/.dbt

# Optional: Slack Notifications (for alerts)
SLACK_WEBHOOK_URL=

# Optional: Email Alerts
ALERT_EMAIL=your-email@example.com
